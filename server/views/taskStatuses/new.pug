extends ../layouts/application.pug

block header
  = t('views.taskStatuses.new')

block content
  +formFor()(action=route('statuses') method='post')
    +input(taskStatus, 'name', errors)(type="text")
    +submit(taskStatus)(value=t('views.taskStatuses.submit'))
